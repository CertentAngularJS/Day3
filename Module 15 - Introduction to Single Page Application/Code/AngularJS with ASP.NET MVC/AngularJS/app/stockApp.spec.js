describe("The StockApp Module", function () {
    var module = angular.module("stockApp");

    it("has been created", function () {
        expect(module).not.toBe(null);
        expect(module).toBeDefined();
    });
});